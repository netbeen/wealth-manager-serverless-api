service:
  name: wealth-manager-serverless-api

provider:
  name: aliyun
  runtime: nodejs14
  memorySize: 512
  timeout: 600 # 单位秒
  region: cn-hangzhou

aggregation:
  all:
    timeout: 600 # 单位秒
    memorySize: 512
    functionsPattern:
      '*'
