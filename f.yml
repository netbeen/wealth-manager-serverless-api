service:
  name: wealth-manager-serverless-api

provider:
  name: tencent
  runtime: nodejs12
  timeout: 60 # 单位秒
  region: ap-hongkong
  serviceId: service-0d8643vs

aggregation:
  all:
    functionsPattern:
      '*'
