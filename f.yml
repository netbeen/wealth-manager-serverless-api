service:
  name: wealth-manager-serverless-api

provider:
  name: aliyun
  runtime: nodejs16
  memorySize: 512
  timeout: 600 # 单位秒
  region: cn-hongkong

aggregation:
  all:
    timeout: 600 # 单位秒
    memorySize: 512
    functionsPattern:
      '*'
